name: venered_social
description: Aplicación de red social tipo Instagram/Threads en Flutter.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # DEPENDENCIAS ACTUALIZADAS
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0 # Actualizado desde 2.5.0
  image_picker: ^1.2.1      # Actualizado desde 1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # Añadido para buenas prácticas

flutter:
  uses-material-design: true
